pipeline {
echo "Hello world"
  agent any
  stages {
    stage("Build") {
      echo 'Build' 
      if(isUnix()){
        sh 'gradle build --info'
        }
        else{
          bat 'gradle build --info'
        }
      }
      stage("Checkpoint") {
     
        checkpoint 'Completed Build'
      
    }
      stage("Deploying"){
              echo 'Deploying'
        bat 'gradle uploadArchives --info'
      
     }
  }
}
